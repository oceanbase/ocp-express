import { createStyles } from 'antd-style';

const useStyles = createStyles(() => {
  return {
    topSqlAdvanceSearchGroup: {
      '.ant-form-item': { marginBottom: '12px' },
    },
  };
});
export default useStyles;
